# CHANGELOG — ระบบจัดส่ง KMMH

## บังคับเลือกประเภทงานก่อนบันทึกบิล (index v1.20) — 26 ส.ค. 2569

**ที่มา:** บิล `RCV541511` (28 ก.ค.69 · แซ้งค์) เป็นงานรับสินค้าแต่ขึ้นค่าเที่ยว ฿40 ทั้งที่กฎคือ ฿0
ไล่โค้ดย้อนหลังถึง พ.ค. พบว่ากฎยกเว้นถูกต้องมาตลอด — ต้นเหตุจริงคือ **คนกรอกไม่ได้กดปุ่มประเภทงานเลยสักปุ่ม**

### ทำไมไม่กดแล้วพัง
`fDeliveryType` เป็น hidden input ที่ **ไม่มีค่าเริ่มต้น** (และ `clearForm()` ล้างกลับเป็น `''` ทุกครั้งหลังบันทึก)
พอค่าเป็น `''` มันไหลผิดพร้อมกัน 2 ทาง:
1. ด่าน "กรุณากรอกเลขที่บิล" ไม่ทำงาน เพราะเช็กเฉพาะ `delivType==='deliver'`
2. ระบบออกเลขบิลให้เองด้วยกฎ "ไม่ใช่ UPK → RCV" แล้ว **คิดค่าเที่ยวเต็ม 40/20** เพราะ `''` ไม่เข้ากฎยกเว้น (upk/receive/klang)

ผลคือได้บิล `RCV` + ค่าเที่ยวเต็ม + ปลายทางว่าง — ตรงกับ RCV541511 ทุกจุด
🔎 หลักฐานปิดเคส: เลขบิล `RCV541511` = 6 หลักท้ายของ `Date.now()` ส่วน `id` ของบิลลงท้าย `541512` — ห่างกัน 1 มิลลิวินาที = ออกเลขตอนกดบันทึก ไม่ใช่ตอนเลือกประเภทงาน

### แก้
`saveStep1()` เพิ่มด่านแรกสุด: ไม่ได้เลือกประเภทงาน = **บันทึกไม่ได้** + เตือน + เลื่อนจอไปที่ปุ่มเลือกประเภทให้

### ทดสอบ (Chromium + ชิม supabase)
- **12 เคสใหม่ผ่านครบ** · โค้ดเก่าพัง 4 เคสตามคาด — ของเก่าสร้างบิล `RCV334477` ค่าเที่ยว 40/20/40 ออกมาจริง (รูปเดียวกับ RCV541511 เป๊ะ)
- ของเดิมไม่พัง: ส่งลูกค้าไม่ใส่เลขบิลยังเตือนเหมือนเดิม · รับสินค้า/ส่งคลังยัง ฿0 · ส่งลูกค้าปกติยัง ฿40
- ชุดเทสต์ v1.19 (กล่องแก้ค่าเที่ยว/โบนัส) รันซ้ำ **ผ่าน 32/32** ไม่มีอะไรถอยหลัง

**ข้อมูลเก่า:** `RCV541511` แก้เป็น ฿0 ในฐานข้อมูลแล้ว (฿40 ที่จ่ายไปในรอบ ก.ค. คุณหลิงเคาะว่าปล่อย ไม่หักคืน)


## แก้ค่าอิฐ/เครน/ลงของ ของบิลที่บันทึกไปแล้วได้ (index v1.19) — 26 ส.ค. 2569

**ที่มา:** บิล KM6908-1102 คนขับลงอิฐบล็อก 400 ก้อน แต่ตอนกดออกรถไม่ได้ติ๊กชิป 🧱 → ค่าอิฐ 100 ฿ หายทั้งก้อน
พอจะแก้ย้อนหลัง ปรากฏว่า **หน้า "✏️ แก้ไขข้อมูลบิล" แก้ได้แค่ เลขบิล/ปลายทาง/วันที่/หมายเหตุ**
เรื่องเงินแก้ไม่ได้เลย ต้องให้คนเข้าไปแก้ในฐานข้อมูลให้ทุกครั้ง

### แก้อะไร
- **DB:** RPC ใหม่ `update_delivery_bonus(rec_id, p_brick_on, p_brick_qty, p_crane, p_labor, p_edited_by)`
  — คิดเงินใหม่ทั้งชุด (ค่าอิฐ · ต่อคน · โบนัส · คนขับ · ผู้ช่วย · รวมค่าเที่ยว) แล้วคืนยอดกลับมาให้หน้าเว็บ
- **เว็บ:** โมดัลแก้ไขบิลมีกล่อง 💰 ค่าเที่ยว/โบนัส — ติ๊กอิฐ+ใส่จำนวนก้อน · เครน · ลงของ
  พร้อมพรีวิว "ยอดใหม่ (เดิม ฿xx)" ให้เห็นก่อนกดบันทึก และเขียนลง log การแก้ไขว่าเปลี่ยนจากเท่าไหร่เป็นเท่าไหร่

🔑 **ยอดเงินคิดที่ RPC เท่านั้น** — หน้าเว็บส่งขึ้นไปได้แค่ "จำนวนก้อน + ติ๊กอะไรบ้าง" ห้ามส่งตัวเลขเงิน
(รีโปเป็น public + PIN อยู่ในโค้ด ถ้ายอมรับยอดเงินจากหน้าเว็บ ใครก็ยิงแก้ค่าเที่ยวตัวเองได้)
RPC เป็น `security definer` และ **grant ให้ `authenticated` เท่านั้น ไม่ให้ anon**

### กฎที่ RPC บังคับซ้ำอีกชั้น (หน้าเว็บซ่อนให้แล้วก็จริง แต่กันคนยิงตรง)
- จำนวนก้อนติดลบ / เกิน 100,000 → ปัดเข้าในช่วง · ไม่ติ๊กอิฐ = 0 ก้อนเสมอ
- เครน/ลงของ **เฉพาะรถ 🚛 84-5763** และเลือกได้อย่างเดียว (ติ๊กทั้งคู่ = เอาเครน)
- งานส่งคลัง = ฿0 ล้วน · งาน UPK/รับสินค้า = ไม่มีค่าเที่ยวพื้นฐาน
- บิลที่ติดป้ายส่งไม่ครบ/ส่งผิด = หักครึ่งให้อัตโนมัติ · บิลที่ยกเลิกแล้ว = แก้ไม่ได้

### ทดสอบ
- RPC: 8 เคสด้วย `begin … rollback` (ปกติ · ติดลบ · ไม่ติ๊กแต่ส่งจำนวน · เครน+ลงของพร้อมกัน · รถกระบะ+ส่งไม่ครบ · ส่งคลัง · บิลยกเลิก · id มั่ว) ผ่านครบ
- หน้าเว็บ: 32 เคสด้วย Chromium + ชิม supabase — **ผ่าน 32/32 · โค้ดเก่าพัง 28 เคส**


## เก็บ 3 บั๊กเล็กที่ค้างจากผลตรวจ 20 ส.ค. (index v1.18) — 21 ส.ค. 2569

**ที่มา:** ผลตรวจเต็มระบบ 20 ส.ค. เจอ 12 บั๊ก แก้ขึ้น live ไป 9 · รอบนี้เก็บ 3 ตัวที่เหลือให้จบ

### 1. ป้าย "👑 เจ้าของยกเลิก" / "✏️ แก้ไขแล้ว" หายหลังรีเฟรช
ป้ายสองอันนี้หายเพราะคนละสาเหตุ
- **ป้ายยกเลิก:** DB มี `cancelled_by`/`cancelled_at` ครบ แต่ `dbToRec()` ไม่ได้อ่านมา → `refreshFromCloud()`
  เอาข้อมูลคลาวด์มาทับ `records` ทุกครั้ง ป้ายเลยหาย (บิลที่เจ้าของยกเลิกจะกลายเป็น "👤 พนักงานยกเลิก")
- **ป้ายแก้ไขแล้ว:** `r.lastEditedAt` **ไม่มีคอลัมน์ใน DB เลย** อยู่แค่ในเครื่อง → โดนทับหายเหมือนกัน
  และเครื่องอื่นไม่มีทางเห็นว่าบิลนี้เคยถูกแก้

**แก้:**
- DB: เพิ่มคอลัมน์ `last_edited_by` (text) · `last_edited_at` (timestamptz) ในตาราง `deliveries` (nullable ไม่กระทบของเดิม)
- DB: RPC `update_delivery` รับ/เขียน 2 ฟิลด์นี้เพิ่ม (ของเดิมทุกบรรทัดคงไว้เหมือนเดิมเป๊ะ)
- เว็บ: `dbToRec()` อ่าน `cancelled_by` · `cancelled_at` · `last_edited_by` · `last_edited_at`
- เว็บ: `confirmEdit()` เขียน `last_edited_*` ขึ้นคลาวด์พร้อมการแก้ไข (ใช้เวลาเดียวกันทั้งบนคลาวด์และในเครื่อง)
- เว็บ: `recToDb()` ส่ง `cancelled_by`/`cancelled_at` ไปด้วย เผื่อบิลที่ยกเลิกตอนเน็ตหลุดแล้วขึ้นคลาวด์ทีหลัง

🔑 **ไม่ต้องแตะ `upsert_delivery`** — 2 คอลัมน์นี้ไม่อยู่ใน SET list ของ ON CONFLICT อยู่แล้ว
แปลว่าเครื่องที่ยังค้างแอปเวอร์ชันเก่า (ไม่ส่งค่ามา) **ลบป้ายของคนอื่นทิ้งไม่ได้** — ทดสอบยืนยันด้วย SQL แล้ว

### 2. เลือกรูปทีเดียวเกิน 6 ใบ ทะลุลิมิตได้
`addPhotos()` / `addPhotosStep2()` เช็ก `length>=6` แบบ sync ในลูป แต่ `push` อยู่ใน callback ของ
`compressImage()` ที่ทำงานทีหลัง → เลือกทีเดียว 10 ใบ ทุกใบผ่านด่านตอน `length` ยังเป็น 0 แล้วค่อยทยอย push = ได้ 10 รูป

**แก้:** ตัดจำนวนไฟล์ตามโควตาที่เหลือตั้งแต่ตอนเลือก (`slots`) + เช็กซ้ำอีกชั้นใน callback
(กันกรณีกดเพิ่มหลายรอบซ้อนกันก่อนรอบแรกบีบรูปเสร็จ) + เตือนบนจอว่ามีรูปถูกข้ามไปกี่ใบ

### 3. วันที่ใช้เวลา UTC → บิลก่อน 07:00 น. เก็บวันย้อนหลัง 1 วัน
`new Date().toISOString().slice(0,10)` ให้วันที่ตามเวลา UTC ซึ่งช้ากว่าไทย 7 ชั่วโมง
ช่องวันที่บนจอโชว์ "21/08/2569" แต่ค่าที่บันทึกจริงเป็น `2026-08-20`

**แก้:** เพิ่ม `isoDate(d)` / `todayISO()` ที่อ่านจากปฏิทินเครื่อง แล้วกวาดแทนที่ **ครบทั้ง 7 จุด**
วันที่บิล · วันที่เติมน้ำมัน · ตัวกรอง "วันนี้" 3 จุด (เตือนอนุมัติ · สรุปวัน · การ์ดหน้าแรก) · ชื่อไฟล์ CSV/backup
(ตรวจย้อนหลัง 120 วันแล้วไม่พบบิลที่โดนจริง = เป็นบั๊กแฝง แก้ก่อนที่จะมีคนเริ่มงานเช้ากว่านี้)

### การทดสอบ
ดึงฟังก์ชันจริงจากไฟล์มารันใน Node (TZ=Asia/Bangkok) **48 เคส ผ่าน 48**
🔑 **เอาชุดเดียวกันไปรันกับโค้ดเก่า v1.17: พังตามคาด 34 เคส ผ่าน 14** — 14 ที่ผ่านคือเคสที่จงใจใส่ไว้ดักว่า
"อย่าทำของเดิมพัง" (เลือกรูป 3 ใบยังได้ 3 · บิลปกติต้องไม่ขึ้นป้าย · ฟิลด์เดิมของ dbToRec/recToDb ต้องค่าตรงเป๊ะ)
= พิสูจน์ว่าเทสต์จับบั๊กได้จริง ไม่ใช่ผ่านลอย ๆ


## รูปกลับมาอยู่บนการ์ดหน้าจองคิว โดยไม่กินเน็ตเหมือนเดิม (queue v2.9) — 20 ส.ค. 2569 (เย็น)

**ที่มา:** คุณหลิงทักว่ารูปในหน้าจองคิวกลายเป็นปุ่ม "แตะดูไฟล์แนบ" · ไล่ประวัติแล้วเกิดจาก `0dfa6ff` (18 ส.ค. · queue v2.7)
ตอนนั้นแก้ egress ด้วยการให้บิลที่ปิดแล้วดึงจาก view `bookings_lite` ที่ไม่มีไฟล์แนบ (เปิดหน้า 1.73 MB → 10.7 KB) — **แต่ไม่ได้บอกว่าหน้าตาจะเปลี่ยนไปด้วย นั่นคือความผิดพลาดของการสื่อสารรอบนั้น**

**ทางสายกลางที่เลือก:** เก็บ "รูปย่อ" ไว้ในบิลด้วย — การ์ดโชว์รูปแค่ 54×54 px จึงไม่ต้องใช้ไฟล์เต็ม
- **ฝั่งเว็บ:** `_makeThumb()` ย่อรูปเหลือกว้าง 96px คุณภาพ 0.55 ตอนแนบไฟล์ → เก็บใน `attach.thumb` ไปพร้อมบิล
  การ์ด: มีรูปย่อ = **โชว์รูปเลย** (แตะที่รูป = โหลดรูปเต็ม) · ไม่มีรูปย่อ/เป็น PDF = ปุ่มเดิม ไม่พัง
  หน้ารายละเอียด: โชว์รูปย่อไปก่อน + ปุ่ม "ดูรูปเต็ม"
- **ฝั่ง DB:** view `bookings_lite` เพิ่มคอลัมน์ `attach_thumb` (คงค่า `security_invoker=true` + ถอนสิทธิ์เขียนของ authenticated/anon เหมือนเดิม)
- **ไล่ทำรูปย่อของเก่าครบแล้ว 215 ใบ** (PDF 4 ใบทำไม่ได้ = ยังเป็นปุ่มเหมือนเดิม) · ทำผ่านเบราว์เซอร์ที่ล็อกอินอยู่ ผ่าน RPC ชั่วคราวที่ **ลบทิ้งแล้วหลังใช้เสร็จ**
  ✔ ตรวจหลังทำ: ไฟล์แนบเดิมครบ 219 ใบ ไม่เสียหายสักใบ (24 MB เท่าเดิม) · รูปย่อเฉลี่ย **3.17 KB** ใหญ่สุด 5.1 KB · รวมกินที่เพิ่ม 681 KB

**ราคาที่จ่าย (วัดจริงบนเว็บ):** เปิดหน้า 1 ครั้ง ดึงบิลปิดแล้ว 12 ใบ **5.2 KB → 43.1 KB** (รวมทั้งหน้า ~10.7 KB → ~86 KB)
ยังดีกว่าก่อน v2.7 ประมาณ **20 เท่า** (เดิม 1.73 MB) · ถ้าเปิดวันละ ~20 ครั้ง = เพิ่ม ~1.5 MB/วัน จากฐาน ~11 MB/วัน

**พ่วงเรื่องความปลอดภัย:**
- รูปย่อถูกยัดลง `src="..."` ตรงๆ → เพิ่มด่าน `_isThumb()` รับเฉพาะ `data:image/...;base64,...` ยาวไม่เกิน 60KB (กันสตริงที่แอบใส่ `"` เพื่อหลุดออกจาก attribute)
- **ชื่อไฟล์แนบเดิมไม่เคยถูก escape** ก่อนลง HTML (ของเก่าตั้งแต่ v2.7) → ใส่ `escHtml()` แล้ว

**ทดสอบ:** ดึง `card()` ตัวจริงจากไฟล์ที่จะ deploy มารันใน Node **ผ่าน 29/29 เคส** · เอาชุดเดียวกันไปรันกับโค้ดเก่า **พังตามคาด 6 เคส**

## เลิก "ขึ้นสำเร็จ" ทั้งที่ระบบไม่ขยับ + หน้าติดตามลูกค้าเร็วขึ้น 130 เท่า (index v1.17 · track v1.1) — 20 ส.ค. 2569 (เย็น)

### ① คำสั่ง 7 จุดที่ล้มแล้วเงียบ — `db.rpc()` / `db.from()` ไม่เช็ค `res.error`
- **ต้นตอ:** supabase-js v2 **ไม่ throw** เวลาผิดพลาด มันคืน `{data, error}` เฉยๆ · โค้ดเดิมครอบ `try/catch` ไว้ก็จริง แต่ `catch` **ไม่มีวันทำงาน** → แอปเด้ง "✓ สำเร็จ" ทุกครั้งแม้ DB ไม่ขยับเลย
- **สถานการณ์จริงที่เจ็บสุด:** session หมดอายุ → แอดมินกด **"อนุมัติทั้งหมด"** → จอขึ้น "✓ อนุมัติ 12 บิลแล้ว" แต่ DB ยัง `pending` ทั้งหมด → **บิลหลุดจากยอดค่าเที่ยวที่ส่งเข้า payroll** โดยไม่มีใครรู้จนกว่าจะปิดงวด
- **แก้:** เพิ่มด่านกลาง `dbRun()` — `await` ผลจริงแล้วเช็ค `res.error` ถ้ามีให้โยนเป็น `Error` เพื่อให้ `catch` ข้างนอกทำงานได้จริง แล้วปรับ 7 จุดตามลักษณะงาน:
  - **5 จุดที่ผู้ใช้กดเอง** (`approveBill` · `approveAll` · `restoreBill` · `confirmEdit` · `confirmDel`) → สลับลำดับเป็น **"ขึ้นระบบสำเร็จก่อน ค่อยเปลี่ยนในเครื่อง"** · ไม่สำเร็จ = **ไม่เปลี่ยนอะไรเลย** + เด้งข้อความแดงบอกสาเหตุให้กดใหม่ · สิ่งที่เห็นบนจอ = สิ่งที่อยู่ในระบบเสมอ
    - `approveAll` แยกผลรายใบ: ใบที่ผ่าน = อนุมัติจริง · ใบที่พัง = ยังค้างรออนุมัติให้กดซ้ำ + บอกจำนวน "สำเร็จ N · ไม่สำเร็จ M"
    - `confirmDel` ย้าย `tgCancelBill()` มาไว้ **หลัง** ขึ้นระบบสำเร็จ — เดิมยิงก่อน ทำให้กลุ่มหน้างานเห็น "❌ ยกเลิกบิล" ทั้งที่ระบบยังนับบิลนั้นอยู่
    - `confirmEdit` / `confirmDel` ไม่สำเร็จ = โมดอลยังเปิด กดบันทึกซ้ำได้เลย ไม่ต้องกรอกใหม่
  - **2 จุดที่เป็นผลพลอยได้ตอนบันทึกบิล** (`commitQueueBooking` · `markBookingDone`) → บล็อกไม่ได้เพราะบิลจัดส่งบันทึกไปแล้ว จึง **ลองใหม่ได้ถึง 3 รอบ ห่างรอบละ 2 วิ** สำเร็จรอบไหนก็จบ เงียบไป · ยังไม่ได้จริงค่อยเด้งเตือนพร้อมเลขบิล
- พ่วง: `_bookingBillIndex()` ทางถอย (fallback) เดิมคืนลิสต์ว่างเมื่อพัง = แอปเข้าใจว่า "บิลนี้ไม่มีคิวจอง" แล้วข้ามไปเงียบๆ · ตอนนี้โยน error ให้ตัวเรียกจัดการ
- ✅ ด่านสิทธิ์เดิมทุกอันยังอยู่ครบ (คนขับอนุมัติ/กู้คืนไม่ได้ · ไม่กรอกเหตุผลแก้ไขไม่ได้)

### ② หน้าติดตามของลูกค้า (track.html) ช้า 1.9–2.7 วินาที และรีเฟรชไม่มีวันหยุด
- **หลักฐาน:** `explain (analyze, buffers)` ของ query แรกใน RPC `track_delivery` = **Seq Scan 2,750 ms อ่าน 3,877 buffer (~30 MB)** เพื่อหาบิลใบเดียว · วันที่ 20 ส.ค. มี **500 statement timeout 2 ครั้ง** (11:30 / 13:33) ตรงกับตอนที่ DB ยุ่ง = หน้าลูกค้าพังจริง
- **ต้นตอ:** เงื่อนไข `where upper(btrim(coalesce(data->>'bill','')))=...` ต้อง **detoast คอลัมน์ `data` ทุกแถว** ซึ่งมีไฟล์แนบ base64 อยู่ข้างใน (ตาราง 245 แถว แต่กินพื้นที่ 28 MB) · planner เห็นว่าตารางเล็กเลยประเมิน cost แค่ 9.88 ไม่รู้ว่าของจริงหนัก
- **แก้ 2 ทาง:**
  1. **ฝั่ง DB:** เพิ่ม index บนนิพจน์เลขบิล — `idx_bookings_bill_upper` และ `idx_deliveries_bill_upper` (ไม่ต้องแก้ RPC เลยสักบรรทัด) → **2,750 ms → 0.13 ms** · ทั้ง RPC วัดจริง **21 ms** (เดิม 1,900–2,750 ms)
  2. **ฝั่งหน้าเว็บ:** นาฬิการีเฟรช 60 วิ **หยุดเองเมื่องานจบ** (ส่งสำเร็จ/ยกเลิก) — เดิมยิงต่อไม่มีวันหยุดทั้งที่สถานะไม่มีทางเปลี่ยนอีกแล้ว (log ยืนยันมีแท็บหนึ่งยิง **1,440 ครั้ง/วัน**) · เพิ่มด่าน `document.hidden` ด้วย แท็บที่พับไว้ไม่ต้องยิง แล้วดึงสถานะล่าสุดทันทีตอนกลับมาดู
- ✅ ตรวจแล้วผลลัพธ์เหมือนเดิมเป๊ะ: ไล่เทียบทั้ง **237 เลขบิล** ระหว่างเส้นทาง index กับ seq scan = **ไม่ต่างกันสักใบ**

**ทดสอบ:** ดึงฟังก์ชันจริงออกจากไฟล์ที่จะ deploy มารันใน Node — **ผ่าน 82/82 เคส** (index.html 65 · track.html 17) · และเอาชุดทดสอบเดียวกันไปรันกับ**โค้ดเก่า พังตามคาด 29 เคส** = ชุดทดสอบจับบั๊กได้จริง ไม่ใช่ผ่านลอยๆ

## อุด 3 ช่องที่ทำข้อมูลหาย (index v1.16 · queue v2.8) — 20 ส.ค. 2569 (บ่าย)
ทั้ง 3 เรื่องยังไม่เคยเกิดจริงในสนาม แต่เปิดช่องไว้และถ้าเกิดคือกู้ไม่ได้

### ① บิลที่บันทึกตอนเน็ตหลุด หายถาวรเมื่อเปิดแอปใหม่
- **ต้นตอ:** `refreshFromCloud()` เอาข้อมูลคลาวด์มาทับทั้ง `records` และ `localStorage` โดยไม่เก็บบิลที่มีเฉพาะในเครื่องไว้เลย
  ฝั่งน้ำมัน (`loadFuelFromCloud`) ทำถูกอยู่แล้ว (คง `localOnly` ไว้แล้ว concat กลับ) — ฝั่งบิลคือจุดที่ลืม
- **สถานการณ์:** คนขับอยู่จุดอับสัญญาณ → กดบันทึกก่อนออกรถ → เด้ง "⚠️ บันทึก Cloud ไม่สำเร็จ **ข้อมูลอยู่ในเครื่อง**" (อ่านแล้วสบายใจ) → กลับเข้าเขตสัญญาณ → เปิดแอปใหม่ → **บิลหายเกลี้ยงทั้งจากจอและจากเครื่อง กู้ไม่ได้** รูปที่อัปขึ้นไปแล้วกลายเป็นไฟล์กำพร้า
- **แก้:** เพิ่ม `mergeLocalOnly(fresh)` เรียกต่อจาก `mergePendingPhotos` — บิลที่คลาวด์ยังไม่มีจะถูกคงไว้ + **ลองส่งขึ้นใหม่อัตโนมัติ** + เด้งเตือนให้คนขับรู้ว่ายังมีบิลค้าง (อย่าเพิ่งล้างข้อมูลเครื่อง) และจำ id ไว้ใน `_localOnlyIds` ให้ปุ่มซิงค์ใช้ต่อ
- ✅ ปลอดภัย: ทั้งไฟล์ไม่มีที่ไหนลบบิลออกจาก `records` เลย (ยกเลิก = soft cancel เก็บแถวไว้) บิลที่ลบแล้วจึงไม่มีทางฟื้น

### ② หน้าจองคิว 4 วินาทีแรก = ด่านกันไฟล์แนบไม่ทำงาน
- **ต้นตอ:** หน้าเปิดมาใช้ `state` จาก localStorage ซึ่งเป็นสำเนาที่ **ตัด `attach` ทิ้งแล้ว** (เพราะ 5MB ไม่พอ) · ถ้าคลาวด์มาไม่ทัน 4 วิ นาฬิกาจะปลดล็อกหน้าจอให้ใช้งานได้ทั้งที่ `_liteIds`/`_cloudSnap` **ยังว่าง** → `save()` ครั้งเดียวจะ upsert ทั้งชุดขึ้นไปโดยไม่มีไฟล์แนบ = **ไฟล์แนบหายยกชุด + สถานะเก่าทับของสดที่เครื่องอื่นเพิ่งเปลี่ยน**
- ด่านกัน 2 ชั้นที่ทำไว้ 18 ส.ค. พึ่ง `_liteIds` ตัวเดียวกันทั้งคู่ จึงไม่ครอบช่วงเวลานี้
- **แก้:** เพิ่มธง `_cloudLoaded` (ตั้ง true เฉพาะตอน `loadCloud` ได้ข้อมูลจริง) → `save()` **ไม่ยิงขึ้นคลาวด์เลย** จนกว่าจะโหลดครบ + ขึ้นแถบแดงบอกพนักงานตรงๆ ว่า "ตอนนี้ยังบันทึกขึ้นระบบไม่ได้ กรุณารีเฟรช"
- ปุ่มที่เปลี่ยนข้อมูลจริง (จองคิว / ยกเลิก-เลื่อน / ส่งสำเร็จ / กลับมารอจัดส่ง) เพิ่มด่าน `requireCloud()` — เตือนแล้วไม่ทำ ดีกว่าทำแล้วหายเงียบ (โดยเฉพาะการจองใหม่ที่จะดูเหมือนจองแล้วแต่ไม่เข้าระบบ)
- ✅ ด่านเดิมยังทำงานครบ: บิลที่ยังไม่โหลดไฟล์แนบยังถูกข้าม และไม่มีอะไรเปลี่ยนก็ยังไม่ยิงซ้ำ (ประหยัดเน็ตเท่าเดิม)

### ③ ปุ่ม ☁️ ซิงค์ทั้งหมด ย้อนงานที่คนอื่นทำระหว่างวัน
- **ต้นตอ:** วน `upsert_delivery` ทุกบิลจากข้อมูลในเครื่องโดยไม่ดึงของใหม่มาเทียบก่อน · แอปไม่มี `setInterval` ดึงข้อมูลใหม่เลย เครื่องที่เปิดค้างไว้ตั้งแต่เช้าจึงถือข้อมูลเช้าอยู่ · `upsert_delivery` ทับทุกคอลัมน์หลัก → **`step2_done` กลับเป็น false, เวลาถึง/พิกัด/รูปหลังส่งหาย** ลิงก์ติดตามที่ส่งให้ลูกค้าแล้วจะไม่มีรูป
- **แก้:** ดึงของใหม่ลงมาก่อน (`refreshFromCloud`) แล้วส่งขึ้นเฉพาะ **บิลที่คลาวด์ยังไม่มี** + รูปที่ยังค้าง
- พ่วง: **เช็ก `res.error` จริง** — supabase-js v2 ไม่ throw ของเดิม `catch` จึงไม่มีวันทำงาน กดแล้วขึ้น "ซิงค์สำเร็จ" ทุกครั้งแม้ล้มทั้งหมด · ตอนนี้ล้มจริงจะเตือนจริง และบอกจำนวนที่ส่งได้จริง

**ทดสอบ:** ดึงฟังก์ชันจริงออกจากไฟล์ที่จะ deploy มารันใน Node **ผ่าน 20/20 เคส** — บิลเน็ตหลุดไม่หาย/ถูกส่งซ้ำให้ · ไม่มีบิลค้าง = ไม่เตือนหลอก · บิลยกเลิกไม่ฟื้น · ปุ่ม ☁️ ไม่ยิงทับสักบิลเมื่อไม่มีอะไรค้าง · ล้มแล้วเตือนจริง · หน้าจองคิวไม่ยิงขึ้นตอนยังโหลดไม่เสร็จ · ด่านไฟล์แนบเดิมยังทำงาน

## เลิกอัปรูปซ้ำลง Storage (index v1.15) — 20 ส.ค. 2569
- **อาการ:** ถัง `delivery-photos` มี **รูปซ้ำ 324 ชุด = 116 MB (19% ของ 610 MB)** และมี 5 บิลที่เปิดดูแล้วเห็นรูปเดิมซ้ำ 2 ใบ (บิลหนึ่งมีรูปหลังส่ง 12 ใบ) · ยังเกิดอยู่จนถึง 17–18 ส.ค.
- **ต้นตอ:** `uploadPhotoToStorage` ตั้งชื่อไฟล์ด้วย `Date.now()` **ทุกครั้งที่ถูกเรียก** — คำว่า "ใช้ path เดิม + x-upsert กันไฟล์ซ้ำ" จริงเฉพาะการ retry ภายในการเรียกครั้งเดียว พอ `syncPendingPhotos` มาเรียกใหม่ (ตั้งนาฬิกาไว้ 20/90/180/300 วิ + ตอนเปิดแอปอีก 1) รูปเดิมจะได้ชื่อใหม่ = ไฟล์ใหม่ทุกรอบ · แล้ว trigger `trg_link_delivery_photo` ที่ประกอบรายการรูปจาก storage ก็ใส่ทั้งคู่เข้าบิล
- **แก้ 3 จุด:**
  1. ชื่อไฟล์คิดจาก **เนื้อรูป** (`_photoTag` = ความยาว + แฮช 2 ตัว) แทนเวลา → รูปเดิมได้ชื่อเดิม อัปซ้ำกี่รอบก็ทับที่เดิม · รูปคนละใบได้คนละชื่อ ไม่ทับกัน
  2. `syncPendingPhotos` เพิ่มตัวกันวิ่งซ้อน (`_syncPhotosBusy`) — นาฬิกาหลายตัวยิงพร้อมกันจะทำงานแค่ตัวเดียว
  3. `syncPendingPhotos` เลิกจำตำแหน่ง `records[i]` ข้าม `await` เปลี่ยนเป็นค้นจาก `id` ใหม่ทุกครั้ง — เดิมถ้าข้อมูลถูกรีเฟรชกลางคัน รูปของบิลหนึ่งอาจไปแปะอีกบิลแล้วเขียนขึ้นคลาวด์ถาวร
- ⚠️ ยังมี `_` คั่นท้ายลำดับรูปเหมือนเดิม (`before_<id>_<ลำดับ>_<ป้าย>.jpg`) — จำเป็นสำหรับ trigger ที่ผูกรูปเข้าบิล ตรวจแล้วรูปแบบชื่อยังผ่าน regex และอ่านลำดับรูปได้ถูก
- **ทดสอบ:** รูปเดิมอัป 6 รอบ = ได้ชื่อเดียว ✅ · รูปต่างกัน 3,000 ใบ = ไม่มีชื่อชนกันเลย ✅ · ต่างกันแค่ตัวอักษรเดียวก็แยกออก ✅ · ความเร็ว ~1.4 ms/รูป
- 📌 **ของเก่า 116 MB ที่ซ้ำอยู่แล้วยังอยู่** — ต้องล้างแยกอีกรอบ (จะทำรายการมาให้ตรวจก่อนลบ)

## ค่าเที่ยวรายคนในรายงาน/Excel ตรงกับยอดจ่ายจริง (index v1.15) — 20 ส.ค. 2569
- **อาการ:** ในหน้ารายงานเดือนเดียวกัน การ์ด "ค่าเที่ยวรวม" กับกราฟ "ค่าเที่ยวรายคน" ไม่ตรงกัน และไฟล์ Excel ที่ส่งให้บัญชี Sheet 1 กับ Sheet 2 ก็ไม่ตรงกัน
- **ต้นตอ:** ทั้ง `renderReport` และ `exportExcel` **ไม่ได้อ่านยอดที่บันทึกไว้** แต่คิดเรตใหม่เองเป็น `คนขับ 40 · ผู้ช่วย 20` ตายตัว จึงไม่รู้จักกติกาประเภทงาน (ส่งคลัง = ฿0 ล้วน · รับสินค้า/UPK = ไม่มีค่าเที่ยวพื้นฐาน) และ Excel ยัง**ไม่หัก 50%** ของบิลที่ติดธง ส่งไม่ครบ/ส่งผิด ด้วย
- **ตัวเลขจริงที่เพี้ยน:** มิ.ย. ฿4,370 → กราฟโชว์ฐาน ฿5,540 · **ก.ค. ฿5,282.50 → ฿7,120 (เกิน 35%)** · ส.ค.(ถึง 19) ฿2,455 → ฿3,660
- **แก้:** เพิ่มฟังก์ชันกลาง `payParts(r)` แยก "ค่าเที่ยวพื้นฐาน" กับ "โบนัส" ออกจาก `driverTotal`/`helperTotal` ที่บันทึกไว้จริง (โบนัส = `brickPP + extraPP` คูณตัวหัก แล้วที่เหลือคือฐาน · กันโบนัสเกินยอดจริงด้วย `min`) แล้วให้ทั้งกราฟรายคนและ Excel Sheet 2 เรียกตัวเดียวกัน
- การ์ด "โบนัสรวม" ในหน้ารายงานก็ใส่ตัวหัก 50% ให้แล้ว (เดิมไม่หัก ทำให้ขัดกับกราฟข้างล่างที่หัก)
- 🔑 **กติกาที่ต้องจำ: อย่าคิดเรตค่าเที่ยวเองอีกไม่ว่าที่ไหน — อ่านจาก `driverTotal`/`helperTotal` เสมอ** ที่ผ่านมาแก้กติกาประเภทงานไปแล้ว 2 จุด (ตอนคำนวณโชว์ + ตอนบันทึก) แต่ลืมจุดที่ 3 คือฝั่งรายงาน
- **ทดสอบ:** ดึงบิลจริงทั้ง 3 เดือนมาเทียบ — ยอดรวมของกราฟใหม่ **ตรงกับการ์ด "ค่าเที่ยวรวม" เป๊ะทุกเดือน** (4,370 / 5,282.50 / 2,455) · เคสย่อยผ่านครบ: ส่งคลัง ฿0 · UPK+ผู้ช่วย ฿0 · บิลหัก 50% ได้ครึ่งเดียว · บิลอิฐ+เครนแยกฐาน/โบนัสถูก · เรียกซ้ำได้ผลเท่าเดิม
- ไม่กระทบยอดที่ส่งเข้าโปรแกรมเงินเดือน — เส้นทางนั้น (`computeTripPayRange`) อ่านยอดจริงอยู่แล้วตั้งแต่แรก

## ยอดผีในสรุปเดือน Telegram + แยกประเภทงาน (edge function `monthly-summary`) — 20 ส.ค. 2569
- **อาการ:** ในข้อความสรุปสิ้นเดือน บรรทัด "💰 ค่าเที่ยวรวม" กับ "👤 ค่าแรงรายคน" บวกไม่ลงตัวทุกเดือน
- **ต้นตอ:** โค้ดเขียน `p.pay += (+r.helper_total || 20)` — งานที่ค่าแรงเป็น ฿0 จริง (ส่งคลัง/รับสินค้า/UPK) ค่า `0` เป็น falsy ใน JavaScript จึงตกไปใช้ 20 แทน · ผีเดือนละ **มิ.ย. ฿240 · ก.ค. ฿260 · ส.ค. ฿240**
- **แก้:** `|| 20` → `|| 0` (ฝั่งคนขับใช้ `|| 0` ถูกอยู่แล้ว พลาดเฉพาะฝั่งผู้ช่วย)
- พ่วง: บรรทัด "🚚 ส่งลูกค้า" เดิมนับงาน active ทั้งหมด (ก.ค. โชว์ 134 ทั้งที่ส่งลูกค้าจริง 90) — ตอนนี้แยกตาม `delivery_type` เป็น **ส่งลูกค้า / รับสินค้า / UPK / ส่งคลัง** (บิลเก่าที่ยังไม่มีคอลัมน์นี้เดาจาก prefix เลขบิลให้)
- เพิ่ม `?dry=1` = คำนวณอย่างเดียว ไม่ส่งเข้า Telegram ไว้ทดสอบโดยไม่รบกวนกลุ่ม
- **ทดสอบ:** เรียกจริงด้วย `?ym=2026-07&dry=1` → `balanced: true` (ค่าแรงรายคนรวม ฿5,282.50 = ค่าเที่ยวรวม ฿5,282.50) · แยกประเภทได้ ส่งลูกค้า 90 · รับสินค้า 10 · UPK 7 · ส่งคลัง 27

## หน้าลูกค้าไม่โชว์บิลที่ยกเลิกแล้วว่า "รับออเดอร์แล้ว" (RPC `track_delivery`) — 20 ส.ค. 2569
- **อาการ:** ลูกค้าที่บิลถูกยกเลิกไปแล้ว เปิดลิงก์ติดตามยังเห็น "🟡 รับออเดอร์แล้ว" + ภาพรอจัดสินค้า และหน้ารีเฟรชทุก 60 วิ ตอกย้ำสถานะผิดไปเรื่อยๆ
- **ต้นตอ:** `queue.html` เก็บสถานะยกเลิกเป็น **`'cancel'`** แต่ RPC และ `track.html` รอคำว่า **`'cancelled'`** → ไม่ตรงกัน แล้ว `track.html` ตกไปที่ค่าเริ่มต้น `map.booked`
- **แก้ที่ RPC จุดเดียว** (ครอบบิลเก่าทั้งหมด ไม่ต้อง deploy หน้าเว็บ): รับได้ทั้ง `cancel` / `cancelled` / `canceled` และตัดช่องว่าง/ตัวพิมพ์ก่อนเทียบ — migration `track_delivery_accept_cancel_status`
- **ทดสอบกับบิลจริงทั้งตาราง:** 3 ใบที่เคยโชว์ผิดเป็น "รับออเดอร์แล้ว" ตอนนี้ขึ้น "🔴 ยกเลิกรายการ" ถูกต้อง · ไม่มี regression (จอง→booked · กำลังส่ง→out · ส่งแล้ว→done เหมือนเดิม)
- 🔑 **กติกาที่ต้องจำ: คำสถานะต้องสะกดเหมือนกันข้ามหน้า และ fallback ของสถานะที่ไม่รู้จักไม่ควรเป็นสถานะแรกสุด**

## เลิกเตือนหลอก "บันทึก Cloud ไม่สำเร็จ" (index v1.14) — 19 ส.ค. 2569
- **อาการ:** คนขับกดบันทึกรถออก แล้วบางครั้งเด้ง "⚠️ บันทึก Cloud ไม่สำเร็จ ข้อมูลอยู่ในเครื่อง" **ทั้งที่ข้อมูลเข้าครบทุกอย่าง** (เจอ 2 ครั้งใน 24 ชม. จาก edge_logs — บิล KM6908-0766 และ KM6908-0791 ตรวจแล้วครบทั้งคู่)
- **ต้นตอ:** ตอนบันทึก Step 1 โค้ดยิง `upsert_delivery` ของแถวเดียวกัน **2 ครั้งพร้อมกัน** — `tgNewBill(rec)` (ไม่ await, ข้างในมี `await saveToCloud`) กับ `saveToCloud(rec)` บรรทัดถัดมา ห่างกัน 3–11 ms → Postgres ชนกันเอง ตัวหนึ่งได้ unique violation → PostgREST ตอบ 409 → เข้า catch แล้ว toast แดง
- **แก้ 2 ชั้น:**
  1. `saveToCloud()` **ต่อคิวตาม `rec.id`** — บิลเดียวกันบันทึกทีละครั้ง ไม่ชนกันอีก (บิลคนละใบยังวิ่งขนานปกติ ไม่ช้าลง)
  2. ถ้ายังเจอ `23505`/409 (เครื่องอื่นเขียนพร้อมกัน) **ลองใหม่เงียบๆ 1 ครั้ง** ก่อนค่อยเตือน
- ⚠️ **ไม่ตัด `await saveToCloud` ใน `tgNewBill`/`tgDone` ทิ้ง** — มันมีไว้ให้แถวมีใน DB ก่อน `claimNotify` ถ้าตัดแล้วแจ้งเตือน Telegram จะซ้ำ/หาย
- error จริง (เช่น JWT หมดอายุ) ยังเตือนเหมือนเดิม — ทดสอบครบ 5 เคส: ยิงซ้อน=วิ่งทีละตัว 0 เตือน · เจอ 409=ยิง 2 ครั้ง 0 เตือน · คนละบิล=ขนาน 2 ตัว · พังจริง=เตือน 1 ครั้ง · คิวไม่รั่ว

## บิลปิดแล้วไม่ต้องลากไฟล์แนบมาด้วย (queue v2.7) — 18 ส.ค. 2569
- **ปัญหา:** เปิดหน้าจองคิวทีนึงดึงบิลที่ปิดแล้ว 24 ใบพร้อม base64 รูป/PDF ที่ฝังใน `bookings.data` = **~1.73 MB ทุกครั้ง** (ทั้งตาราง 27 MB / เฉลี่ย 101 kB ต่อแถว) แค่เพื่อโชว์การ์ด
- **แก้:** เพิ่ม view `bookings_lite` (migration `bookings_lite_view_no_attach_20260818`) = `data - 'attach'` + คอลัมน์ `attach_type` / `attach_name`
  → บิลปิดแล้ว 24 ใบเหลือ **10.7 kB (วัดจริงบนสายส่ง 12.4 kB) เล็กลง 161 เท่า** · ปุ่ม "ดูบิลที่ยกเลิกทั้งหมด" 1.1 MB → 8 kB
- ไฟล์แนบโหลดทีหลังใบต่อใบ (`hydrateAttach`) — กดปุ่ม 🖼️ แตะดูไฟล์แนบ บนการ์ด หรือเปิดหน้ารายละเอียดแล้วโหลดให้เอง (~100 kB เฉพาะใบที่ดูจริง)
- 🔑 **กันไฟล์แนบหาย 2 ชั้น** (นี่คือเหตุผลที่แต่ก่อนห้ามแตะ `toggleAllCancel`):
  (1) `save()` ข้ามทุกบิลที่ยังอยู่ใน `_liteIds` ไม่ upsert เด็ดขาด  (2) `restoreBooking()` hydrate ให้ครบก่อนเปลี่ยนสถานะเสมอ ถ้าโหลดไม่สำเร็จจะไม่แก้อะไรเลย
- ธง `_liteIds` / `_attachMeta` เก็บนอกตัวบิล (side map) → ไม่มีคีย์แปลกปลอมหลุดลง `data` และเทียบ `_cloudSnap` ได้สะอาด
- มีทางสำรองทุกจุด: ถ้า view หาย/พัง (`PGRST205`) ถอยไปดึงแบบเดิมอัตโนมัติ — แย่สุดคือเท่าเดิม
- RLS: view เป็น `security_invoker` → `anon` อ่านไม่ได้ (401) `authenticated` อ่านได้เหมือนเดิม
- ทดสอบกับฐานข้อมูลจริงโดยดักไม่ให้เขียนจริง: ไม่แก้ = 0 แถว · แก้บิลที่ยังไม่ hydrate = ถูกข้าม 0 แถว · hydrate แล้วแก้ = เขียน 1 แถวพร้อมไฟล์แนบครบ · ยืนยัน `updated_at` ในฐานข้อมูลไม่ขยับ

## ลิงก์เช็คสถานะในข้อความ "รถออกแล้ว" (index v1.12) — 14 ส.ค. 2569
- ข้อความแจ้งเตือน 🔵 รถออกแล้ว ในกลุ่ม Telegram "จองคิว & จัดส่ง" เพิ่มบรรทัด 🔗 ลิงก์เช็คสถานะ (track.html + โทเคน) ต่อท้าย
- ประโยชน์: ฝ่ายขายกดค้างที่ลิงก์ = คัดลอกส่งเข้า LINE ลูกค้าได้ทันทีจากมือถือ ไม่ต้องเปิดแอปแล้วไล่หาบิลเพื่อกดปุ่ม 🔗 — และเป็นตัวสำรองของบิลที่ลืมส่งลิงก์ตอนจองคิว
- **เฉพาะงานส่งลูกค้า (delivTypeOf==='deliver') เท่านั้น** — ส่งคลัง/รับสินค้า/UPK ปลายทางไม่ใช่ลูกค้า จึงไม่มีลิงก์
- สร้างลิงก์หลังเช็ก claimNotify ผ่านแล้วเท่านั้น (ไม่เสียการเรียก track_token ฟรีเมื่อเครื่องอื่นแจ้งไปแล้ว) และมี try/catch — สร้างลิงก์ไม่ได้ก็ยังส่งแจ้งเตือนตามปกติ
- ไม่ต้องแก้ DB — ใช้ RPC track_token/track_delivery เดิม

## ส่งลิงก์ติดตามให้ลูกค้าได้ตั้งแต่ตอนจองคิว (queue.html) — 12 ส.ค. 2569
- เดิมปุ่ม 🔗 ส่งลิงก์ อยู่แค่ในแอปจัดส่ง (index.html) = ส่งได้ต่อเมื่อบิลเข้าประวัติแล้ว → ลูกค้าไม่เคยได้เห็นภาพ "กำลังจัดสินค้า" (status-wait.gif / stage `booked`) เลย
- เพิ่มในหน้าจองคิว 2 จุด: (1) **หลังกดบันทึกการจอง** เด้งถามเลยว่าจะส่งลิงก์ไหม (เฉพาะงานส่งลูกค้าที่มีเลขบิล) (2) **ปุ่มบนการ์ดคิวทุกใบ** ที่ยังไม่ส่งสำเร็จ/ไม่ยกเลิก
- ยกฟังก์ชัน buildTrackUrl()/copyText() มาจาก index.html + ข้อความใหม่ `buildBookedMessage()` (รับออเดอร์แล้ว + กำหนดส่ง) — ไม่มีข้อมูลภายในหลุด
- 🔑 `save()` คืน Promise แล้ว และรอให้บิลขึ้นคลาวด์ก่อนสร้างลิงก์ ไม่งั้นลูกค้ากดเข้ามาเจอ "ไม่พบบิล"
- งานส่งคลัง/รับสินค้า/UPK ไม่มีปุ่มนี้ (ปลายทางไม่ใช่ลูกค้า) · ฝั่ง DB ไม่ต้องแก้ — `track_delivery` รองรับ stage `booked` จากตาราง bookings อยู่แล้ว

## งานส่งคลัง = ฿0 ล้วน + เก็บประเภทงานลงฐานข้อมูล (index v1.11) — 12 ส.ค. 2569
- **งานส่งคลัง (klang) ไม่คิดเงินใดๆ ทั้งสิ้น**: เดิมตัดแค่ค่าเที่ยวพื้นฐาน (คนขับ ฿40 / ผู้ช่วย ฿20) แต่โบนัสอิฐ/เครน/แรงงานยังคิดอยู่ → บิล ส่งคลัง-1008-001 และ -1108-001 ขึ้น ฿10 (ติ๊กแรงงาน ฿5/คน × 2 คน). ล็อก brickTotal/brickPP/extraPP = 0 ทั้ง calcAll() และ saveDelivery()
- แบนเนอร์แยกข้อความ: ส่งคลัง = "ไม่คิดค่าเที่ยวและไม่คิดโบนัสใดๆ (฿0)" · รับสินค้า/UPK = "ไม่คิดค่าเที่ยวพื้นฐาน" (ยังได้โบนัสถ้าติ๊ก)
- **เก็บ deliveryType ลง DB แล้ว** (คอลัมน์ deliveries.delivery_type + เพิ่มใน SET list ของ upsert_delivery): เดิมอยู่แค่ localStorage → เปิดจากเครื่องอื่น/โหลดจากคลาวด์ ป้ายจะกลายเป็น "🚚 ส่งลูกค้า" ผิดทุกใบ
- เพิ่ม delivTypeOf()/typeLabelOf()/typeBadgeOf() ใช้ร่วมกันทุกจุด + เดาจาก prefix เลขบิลให้บิลเก่า; ป้าย "🏭 ส่งคลัง" ขึ้นครบทั้งการ์ดประวัติ/หน้ารออนุมัติ/Telegram; สรุปเดือนนับส่งคลังแยก; ป้าย ⭐พิเศษ ไม่ขึ้นกับงานส่งคลัง (ไม่มีเงิน)
- ข้อมูลย้อนหลัง: backfill delivery_type ทุกแถวจาก prefix เลขบิล + แก้ 2 บิลส่งคลัง 10-11 ส.ค. เป็น ฿0

## ปรับรายชื่อผู้ช่วย: ออก "โด้" เพิ่ม "ดรีม" (index v1.10) — 8 ส.ค. 2569
- เอาชิปผู้ช่วย "โด้" ออก (ลาออก 17 มิ.ย. 69, K013)
- เพิ่มชิปผู้ช่วย "ดรีม" (K019 เริ่ม 3 ส.ค. 69) — สะกดตรงกับ employees.nickname

## แจ้งเตือน Telegram เมื่อส่งยอดค่าเที่ยวเข้า Payroll — 29 มิ.ย. 2569
- ส่งยอดสำเร็จ → แจ้งกลุ่มสรุปหลังบ้าน: งวด/รอบตัด/คนส่ง/จำนวนคน/ยอดรวม + รายคน (+ หมายเหตุถ้าส่งบางคน/มีคนจับคู่ชื่อไม่ได้)

## งวดแรกเริ่ม 1 มิ.ย. (กันจ่ายซ้ำกับงวด พ.ค.) — 29 มิ.ย. 2569
- ล็อกพื้น TRIP_PAY_FLOOR=1 มิ.ย.69: งวด มิ.ย. แสดง 1-25 มิ.ย. (26-31 พ.ค. จ่ายครบในงวด พ.ค. แล้ว)
- งวดถัดไปกลับเป็นรอบ 26-25 ปกติ — เศษ 26-30 มิ.ย. ไปรวมต้นงวด ก.ค. อัตโนมัติ (ไม่หาย)
- แก้ checkbox เลือกส่งยอดให้แสดงเครื่องหมายถูก (appearance/accent-color)

## ค่าเที่ยว→Payroll: รอบตัดยอด 26-25 + เลือกส่งรายคน + ยอดยกงวดหน้า (index v1.9) — 29 มิ.ย. 2569
- เปลี่ยนการตัดรอบจาก "เดือนปฏิทินเต็ม" เป็น **รอบตัดยอด 26 เดือนก่อน – 25 เดือนนี้** (ส่งยอดทุกวันที่ 25 ให้ HR ตรวจทันจ่ายสิ้นเดือน)
- ยอดวันที่ 26–สิ้นเดือน แสดงแยกเป็น **"⏭️ ยกไปงวดหน้า"** (ไม่ส่งงวดนี้ — ยกไปรวมงวดถัดไป)
- **ติ๊กเลือกส่งยอดเป็นรายคน** (รองรับเคสพนักงานลาออกกลางเดือน) + ปุ่มติ๊กทั้งหมด/ล้าง + ยอดรวมอัปเดตตามที่ติ๊ก
- ส่งเฉพาะคนที่ติ๊ก: ไม่แตะข้อเสนอของคนอื่นที่ส่งไว้ก่อนหน้า (ลบข้อเสนอเก่าเฉพาะตอนส่งครบทุกคน)
- รายงานพิมพ์ตรวจสอบแสดงตามยอดที่ส่ง (เฉพาะคนที่ติ๊ก) + หัวรายงานบอกช่วงรอบตัด + ตารางยอดยกไปงวดหน้า

## หักค่าเที่ยว ส่งไม่ครบ/ส่งผิด + สรุปพลาดรายคนสิ้นเดือน (index v1.8) — 26 มิ.ย. 2569
- หักค่าเที่ยว 50% กรณีส่งไม่ครบ/ส่งผิด (หักทั้งคนขับ + ผู้ช่วยทุกคน รวมโบนัสอิฐ/เครน/แรงงาน)
- กรอกได้ 2 จุด: สวิตช์ตอน Step 2 หน้างาน + ปุ่มในหน้ารายละเอียดบิล (ผู้ใช้ทั่วไปกดได้ ไม่ต้องล็อกอินแอดมิน)
- เลือกประเภท 📦 ส่งไม่ครบ / ❌ ส่งผิด (หัก 50% เท่ากัน) + บังคับใส่เหตุผล + ปุ่มยกเลิกคืนค่าเที่ยวเต็ม
- บันทึก log ทุกครั้ง (ใคร/เหตุผล/เวลา); ยอดหักสะท้อนในรายงานค่าแรงรายคน/สรุปวัน/Excel/Telegram
- เพิ่มคอลัมน์ DB: incomplete, incomplete_reason, penalty_type
- สรุปสิ้นเดือน "ใครพลาดบ่อยแค่ไหน": นับพลาดรายคน (ส่งไม่ครบ/ส่งผิด + ไม่มี GPS ออก + รูปไม่ครบ + บิลค้างไม่ปิด) โผล่ทั้ง Telegram สิ้นเดือน + การ์ด "พลาดบ่อยรายคน" ในหน้ารายงาน

## เลขอ้างอิงงานชุดเดียวกัน 2 แอป + ซิงค์สถานะอัตโนมัติ (index v1.7 / queue v2.1) — 23 มิ.ย. 2569
- รวมสูตรสร้างเลขอ้างอิงงาน receive/upk/klang ให้เหมือนกันทั้ง index.html และ queue.html: PREFIX-DDMM-NNN (UPK / RCV / KLG)
- เดิม index ใช้ UPK+timestamp 6 หลัก, queue ใช้คำไทย "รับสินค้า/ส่งคลัง" → จับคู่ข้ามแอปไม่ได้
- DB trigger trg_sync_booking_from_delivery (deliveries→bookings): บันทึกถึง→ส่งสำเร็จ, ยกเลิก→ยกเลิก ตามเลขบิล + คำนวณระยะห่าง
- DB cron reconcile-booking-delivery 22:30 ทุกวัน = ตาข่ายกันคิว↔จัดส่งไม่ตรงกัน

## เชื่อมจองคิว↔Step1 เฟส 2 — เที่ยวรวมหลายบิล (queue v2.0 / index v1.5) — 20 มิ.ย. 2569
- queue.html: dispatchTrip/dispatchGroup (ออกหลายบิลเที่ยวเดียว) → เขียน handoff {mode:trip, bills:[...]} + status delivering + เด้งไป index.html (เอา Telegram ออก ให้ Step1 แจ้งต่อบิล)
- index.html checkHandoff: ถ้า mode=trip → เปิดโหมดเที่ยวรวม (toggleTripMode) + เติมบิลแรกอัตโนมัติ + โชว์ banner รายการบิลทั้งหมด มีปุ่ม "เติม" (hoFill) ให้กดเติมบิลถัดไปทีละใบ (เลือกรถ/คนขับ/อุปกรณ์ใช้ร่วม กดบันทึกแล้วเติมใบต่อไป) → ค่าเที่ยวรวมเหมือนเดิม (finishTrip)
- ทดสอบจำลอง 3 บิลผ่าน: เปิดโหมด+เติมบิลแรก+ปุ่มเติม+กดเติมบิล2 เติมถูกประเภท

## ตั้งค่าส่วนกลาง + ย้ายเข้าแอดมิน (queue v1.9 / index v1.4) — 20 มิ.ย. 2569
- เพิ่มตาราง qb_config (Supabase, row main) เก็บ จุดร้าน + ระยะเตือน ส่วนกลาง ทุกเครื่องเห็นเหมือนกัน
- queue.html: เอาการ์ด "ตั้งค่า" ออกจากหน้าจอง (พนักงานทุกคนใช้สะอาดๆ), โชว์ "ทิศ 🧭" กลับมา, โหลด store/threshold จาก qb_config ตอนเปิด (มี migrate หมุดเดิมในเครื่องขึ้นกลางอัตโนมัติถ้ากลางยังว่าง)
- index.html หน้าแอดมิน (หลัง PIN): เพิ่มการ์ด "🗺️ ตั้งค่าระบบจองคิว" — ใส่พิกัดร้าน (ลิงก์ Google Maps/lat,lng) + ระยะเตือน → บันทึกลง qb_config (saveQbConfig/loadQbConfig/parseCfgCoords)
- ทดสอบครบ: แอดมินบันทึก → หน้าจองอ่านได้ store+threshold + ทิศโชว์ถูกทิศ

## v1.8 (queue) — 20 มิ.ย. 2569
- ซ่อน UI จุดร้าน + ทิศ 🧭 ทั้งหมด (ปุ่มปักหมุดร้าน, label, ป้ายทิศบนการ์ด/หัวกลุ่ม/รายละเอียด) — เก็บโค้ด dirOf/bearing/store ไว้ ไม่ลบ (display:none) เปิดกลับได้
- คงไว้: ช่อง "ระยะเตือนส่งห่างจากที่จอง" + การเทียบ พิกัดที่จอง ↔ จุดส่งจริง (haversine) ตอนกดส่งสำเร็จ

## index v1.3 — 20 มิ.ย. 2569
- เตือนตอนกดบันทึก Step 1 ถ้ายังไม่ได้ดึง "GPS ออก" (confirm: บันทึกเลย / ยกเลิกไปกดปุ่มดึง GPS ก่อน)
- หมายเหตุ: หมุดร้านในหน้าจองคิว = ใช้คำนวณทิศทางเท่านั้น คนละค่ากับ GPS ออก (พิกัดจริงของเครื่องตอนออกรถ)

## เชื่อมจองคิว↔Step1 เฟส 1 (index v1.2 / queue v1.7) — 20 มิ.ย. 2569
- เพิ่มประเภทงาน "🏭 ส่งคลัง" (klang) เป็นตัวที่ 4 ทั้ง 2 แอป
- กด "ออกส่ง" บิลเดียวในจองคิว → เขียน handoff ลง localStorage (key kmmh_handoff) + เปลี่ยนสถานะบิลเป็นกำลังส่ง + เด้งไป index.html
- index.html: checkHandoff() ตอนโหลด → เปิด Step 1 อัตโนมัติ เติม เลขบิล + ปลายทาง (ชื่อลูกค้า+ลิงก์ Google Maps) + ประเภทงาน → พนักงานเลือก รถ/คนขับ/ผู้ช่วย/อุปกรณ์/รูป เอง → ค่าเที่ยวคิดเหมือนเดิม (calcAll)
- ออกส่งบิลเดียวไม่ส่ง Telegram ซ้ำจากจองคิวแล้ว (ให้ Step1 แจ้ง "รถออกส่ง" แทน) — กันแจ้งซ้ำ
- ยังเหลือ เฟส 2: เที่ยวรวมหลายบิล → Step1 trip mode

## v1.6 (queue) — 20 มิ.ย. 2569
- เพิ่ม "เป้าหมายของการจัดส่ง/ประเภทงาน" ในหน้าจองคิว (ดึงจาก Step 1 ของ index.html): 🚚 ส่งลูกค้า (deliver, ค่าเริ่มต้น) / 📦 รับสินค้า (receive) / 🏢 UPK (upk)
- เก็บใน booking.type, แสดงบนการ์ด + หน้ารายละเอียด + ใส่ในแจ้งเตือน Telegram ตอนจองใหม่
- หมายเหตุ: Step 1 มี 3 ประเภทนี้ (ไม่มี "ส่งคลัง"); ถ้าต้องการเพิ่ม ส่งคลัง ต้องเพิ่มทั้ง 2 แอป

## v1.5 (queue) — 20 มิ.ย. 2569
- คลายลิงก์ย่อ Google Maps อัตโนมัติ: วางลิงก์ maps.app.goo.gl / goo.gl ได้เลย ระบบเปิดตามลิงก์ฝั่งเซิร์ฟเวอร์แล้วดึงพิกัดให้ (edge function resolve-maps, จำกัด host google กัน SSRF)
- parseDestLink: เจอลิงก์ย่อ → "⏳ กำลังคลายลิงก์ย่อ..." → ตั้งพิกัดอัตโนมัติ หรือแจ้งถ้าไม่สำเร็จ

## v1.4 (queue) — 20 มิ.ย. 2569
- กันเลขบิลซ้ำให้แน่นขึ้น: normalize เลขบิล (ตัดช่องว่าง + ตัวพิมพ์ใหญ่) → "km6906-0001 " = "KM6906-0001"
- เช็คซ้ำสดจาก Supabase ทุกเครื่องตอนกดบันทึก (กัน race ข้ามเครื่อง) นอกเหนือจากเช็ค local
- เตือนทันทีระหว่างพิมพ์เลขบิล (checkDupBill): ⚠️ ซ้ำ / ✓ ยังว่าง
- เลขที่เคย "ยกเลิก" ใช้ซ้ำได้ (ไม่บล็อก), สถานะอื่นบล็อก

## v1.3 (queue) + index v1.1 — 20 มิ.ย. 2569
- จัดกลุ่มแจ้งเตือนใหม่: กลุ่ม "จองคิว & จัดส่ง" (-5142584793) รับ จอง/รถออก/ส่งสำเร็จ/ยกเลิก · กลุ่มหลังบ้าน (-5127977749) เหลือแค่ สรุปวัน/เดือน + รออนุมัติ + เติมน้ำมัน
- queue.html v1.3: เพิ่มแจ้ง "ออกส่งสินค้า/ออกเที่ยวรวม" + "ส่งสำเร็จ" → กลุ่มจองคิว
- index.html v1.1: route "รถออกส่ง" + "ยกเลิกบิล" → กลุ่มจองคิว (ผ่าน alias sendTelegramDeliv)
- edge function notify-telegram v2: รับ param chat_id แยกปลายทาง
- ฟีเจอร์ใหม่ "เช็คคิว": ถามในกลุ่ม (พิมพ์ "มีคิวว่างไหม"/"เช็คคิว"/"คิวว่าง" หรือ /queue, /q) → บอทตอบ รอจัดส่ง/กำลังส่ง/รอคนละวัน + รวมติดจอง — edge function telegram-webhook (verify_jwt=false + secret token)
- ⚠️ ต้องปิด Privacy ของบอทใน BotFather (/setprivacy → Disable) เพื่อให้พิมพ์ข้อความธรรมดาในกลุ่มได้

## v1.2 — 20 มิ.ย. 2569
- queue.html: แยกแจ้งเตือนจองคิว ("จองคิวใหม่" + "ยกเลิก/เลื่อน") ไปกลุ่มใหม่ "แจ้งเตือน : จองคิว & จัดส่ง" (chat -5142584793) — แยกจากกลุ่มหลังบ้าน (อนุมัติ/สรุปเที่ยว)
- edge function notify-telegram v2: รับ param chat_id แยกปลายทางได้ (ค่า default = กลุ่มเดิม, backward-compatible)

## v1.1 — 20 มิ.ย. 2569
- queue.html: เชื่อม Telegram — แจ้งเตือนเข้ากลุ่มตอน "จองคิวใหม่" และ "ยกเลิก/เลื่อนวันส่ง" (ผ่าน edge function notify-telegram)

**หลักการนับเวอร์ชัน:** แก้เล็ก/เพิ่มฟีเจอร์ย่อย → ขึ้นทศนิยม (v1.1, v1.2 …) · เปลี่ยนใหญ่/โครงสร้างใหม่ → ขึ้นเลขหลัก (v2.0)

---

## v1.0 — 20 มิถุนายน 2569

### แอปหลัก (index.html)
- บันทึกงานส่ง 2 สเต็ป (ก่อนออกรถ / ถึงปลายทาง), รูปก่อน-หลัง, น้ำมัน, ประวัติ, รายงาน, แจ้งเตือน Telegram
- ปรับรูปบิลให้คมขึ้น (ย่อภาพ 600 → 1600px, คุณภาพ JPEG 0.5 → 0.8)
- เพิ่มการตรวจ + เตือนรูปเบลอตอนถ่าย (พรีวิวให้ดูก่อนใช้)
- รายการน้ำมัน: ปุ่มกาง/ยุบ + กันบันทึกซ้ำ (ทั้งฝั่งแอปและฝั่งเซิร์ฟเวอร์ด้วย DB trigger)
- เพิ่มปุ่ม "จองคิว" ในแถบเมนูล่าง เข้าหน้าระบบจองคิว

### หน้าจองคิว/จัดส่ง (queue.html) — รุ่นแรก
- จองคิวล่วงหน้า: เลขบิล + ปลายทาง (วางลิงก์ Google Maps หรือพิกัด) + เบอร์ + แนบรูป/PDF
- หน้าคิวพนักงานจัดส่ง: เลือกบิลออกส่ง, โหมดจัดเที่ยว (เลือกหลายบิลผูกเป็นเที่ยวเดียว)
- แนะนำส่งด้วยกัน (จับบิลพิกัดใกล้กัน) + จัดกลุ่ม "กำลังส่ง" ตามเที่ยว
- ตรวจระยะ GPS จุดที่จอง vs จุดส่งจริง (เตือนถ้าห่างเกินกำหนด)
- จัดการบิล: ยกเลิกออเดอร์ / เลื่อนวันส่ง / ลูกค้ารับเอง
- เชื่อม Supabase จริง (ตาราง bookings) ซิงค์ทุกเครื่อง
- แสดงวันที่เป็น พ.ศ. ทั้งหมด

> หมายเหตุ: ระบบจองคิว (ตาราง bookings) กับงานส่งเดิม (ตาราง deliveries) ยังเก็บข้อมูลแยกกัน — การเชื่อมข้อมูลข้ามระบบ + แจ้ง Telegram ร่วมกัน เป็นแผนเวอร์ชันถัดไป
